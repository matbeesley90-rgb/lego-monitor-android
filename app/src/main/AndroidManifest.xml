<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Android 14 requires this specific subtype for "remoteMessaging" style FG services -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_REMOTE_MESSAGING" />
    <!-- Notification permission is runtime-requested on Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Full package visibility (Android 11+). Needed so we can detect
         which installed app owns a marketplace domain and launch it,
         bypassing link-hijackers like the My O2 app. This is a sideloaded
         personal app (not on Play), so the broad permission is fine and
         the narrower <queries> approach proved too limited (queries came
         back empty, breaking all four apps). -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.LegoMonitor"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 2026-09-08: opened from the notification's info face. Native
             sheet over whatever is on screen; not a browser page. -->
        <activity
            android:name=".InfoSheetActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.LegoMonitor.Sheet" />
        <!-- Tap on the notification photo. -->
        <activity
            android:name=".PhotoViewerActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.LegoMonitor.Viewer" />

        <!-- 2026-09-09: in-place card redraws (photo size / info block). -->
        <receiver
            android:name=".CardActionReceiver"
            android:exported="false" />

        <service
            android:name=".WebSocketService"
            android:exported="false"
            android:foregroundServiceType="remoteMessaging" />

    </application>
</manifest>
